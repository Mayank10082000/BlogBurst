// Add the logic for pagination in the public blogs page and user dashboard page

import React from "react";

const Pagination = () => {
  return <div>Pagination</div>;
};

export default Pagination;
